package main.matchmaker;

/*
 * This class will contain the algorithm that will match players and put them into a single game
 */

import java.util.ArrayList;


public class Matchmaker
{
    /*
     * This method will do most of the heavy lifting. Given a list of players (minimum 10 to maximum 10000),
     * it has to put together 10 players (based on their preferences, ping and skill level) into a single game.
     * Then it has to return a game object that contains the player names, the map they are going to play in
     * and their skill levels.
     */
    public static Game match(ArrayList<Player> players)
    {
        Game newGame = null;
        
        return newGame;
    }
}
